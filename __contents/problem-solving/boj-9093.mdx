---
title: 백준 9093 - 단어 뒤집기
description:
thumbnail:
tags: ['BaekJoon']
createdAt: 2024-07-25T08:17:21.921Z
---

## 문제 링크

[9093 - 단어 뒤집기](https://www.acmicpc.net/problem/9093)

## 사용 알고리즘

- 스택

## 시간 복잡도

- $O(N)$

## 문제 풀이

공백을 기준으로 문자열을 쪼개진 문자열을 `reverse` 함수나 스택을 활용해서 뒤집어 해결할 수 있는 문제입니다.
C++에서는 `split`을 내장 함수로 제공하지 않아서 직접 구현했습니다.

### C++

```cpp title="C++ - reverse 활용" hl:{22-23}
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
int t;

vector<string> split(string& input, string delimiter) {
  vector<string> ret;
  auto start = 0;
  auto end = input.find(delimiter);
  while (end != string::npos) {
    ret.push_back(input.substr(start, end - start));
    start = end + delimiter.size();
    end = input.find(delimiter, start);
  }
  ret.push_back(input.substr(start));
  return ret;
}

int main() {
  cin >> t;
  string bf;
  getline(cin, bf); // 버퍼에 남은 개행 문자 제거

  while (t--) {
    string s;
    getline(cin, s);

    vector<string> v = split(s, " ");

    for (auto it : v) {
      reverse(it.begin(), it.end());
      cout << it << " ";
    }
    cout << '\n';
  }

  return 0;
}
```

### JavaScript

```js title="JavaScript - 스택 활용"
const fs = require('fs');
const filePath = process.platform === 'linux' ? '/dev/stdin' : './input.txt';
const input = fs.readFileSync(filePath).toString().split('\n');

input.shift();

const solution = (input) => {
  const result = [];

  for (const sentence of input) {
    const words = sentence.split(' ');

    for (const word of words) {
      const stack = [];

      for (const letter of word) {
        stack.push(letter);
      }

      for (_ of word) {
        result.push(stack.pop());
      }

      result.push(' ');
    }
    result.push('\n');
  }

  return result.join('');
};

console.log(solution(input));
```
