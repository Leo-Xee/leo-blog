---
title: 백준 1012 - 유기농 배추
description:
thumbnail:
tags: ['BaekJoon']
createdAt: 2024-10-16T15:17:00.000Z
---

## 문제 링크

[1012 - 유기농 배추](https://www.acmicpc.net/problem/1012)

## 사용 알고리즘

- DFS

## 시간 복잡도

- $O(N*M)$

## 문제 풀이

전체 그래프에서 이어진 그래프의 개수를 찾는 연결된 컴포넌트(Connected Components) 문제입니다.

BFS, DFS 모두 사용할 수 있지만 DFS가 더 간단해서 DFS를 사용했고 DFS를 수행할 때마다 개수를 증감하면 해결할 수 있습니다.

### C++

```cpp title="C++"
#include <bits/stdc++.h>
using namespace std;

const int max_len = 51;
int dy[] = {-1, 0, 1, 0};
int dx[] = {0, 1, 0, -1};
int t, m, n, k, ret, x, y, a[max_len][max_len], visited[max_len][max_len];

void dfs(int y, int x) {
  visited[y][x] = 1;

  for (int i = 0; i < 4; i++) {
    int ny = y + dy[i];
    int nx = x + dx[i];

    if (ny < 0 || nx < 0 || ny >= n || nx >= m) continue;
    if (a[ny][nx] == 0 || visited[ny][nx]) continue;
    dfs(ny, nx);
  }
}

int main() {
  cin >> t;

  for (int i = 0; i < t; i++) {
    memset(a, 0, sizeof(a));
    memset(visited, 0, sizeof(visited));
    ret = 0;
    cin >> m >> n >> k;

    for (int j = 0; j < k; j++) {
      cin >> x >> y;
      a[y][x] = 1;
    }

    for (int i = 0; i < n; i++) {
      for (int j = 0; j < m; j++) {
        if (a[i][j] == 1 && !visited[i][j]) {
          dfs(i, j);
          ret++;
        }
      }
    }

    cout << ret << '\n';
  }

  return 0;
}
```

### JavaScript

```js title="JavaScript"
const fs = require('fs');
const filePath = process.platform === 'linux' ? '/dev/stdin' : './input.txt';
const input = fs.readFileSync(filePath).toString().trim().split('\n');

const t = +input.shift();

let a, visited, m, n, k, x, y, ret;
const dy = [-1, 0, 1, 0];
const dx = [0, 1, 0, -1];

const dfs = (y, x) => {
  visited[y][x] = 1;

  for (let i = 0; i < 4; i++) {
    const ny = y + dy[i];
    const nx = x + dx[i];

    if (ny < 0 || nx < 0 || ny >= n || nx >= m) continue;
    if (a[ny][nx] === 0 || visited[ny][nx]) continue;
    dfs(ny, nx);
  }
};

for (let i = 0; i < t; i++) {
  ret = 0;
  [m, n, k] = input
    .shift()
    .split(' ')
    .map((v) => +v);

  a = [...Array(n)].map(() => Array(m).fill(0));
  visited = [...Array(n)].map(() => Array(m).fill(0));

  for (let j = 0; j < k; j++) {
    [x, y] = input
      .shift()
      .split(' ')
      .map((v) => +v);

    a[y][x] = 1;
  }

  for (let i = 0; i < n; i++) {
    for (let j = 0; j < m; j++) {
      if (a[i][j] === 1 && !visited[i][j]) {
        dfs(i, j);
        ret++;
      }
    }
  }

  console.log(ret);
}
```
